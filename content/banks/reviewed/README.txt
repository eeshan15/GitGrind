Approved questions land here.

When you approve an item in the Bank tab's review queue (or with
tools/ingest.py --approve), it is written into this folder as
<subject>.json and becomes live in practice.

The raw imported file is never edited, so rejecting or re-importing is
always safe.
