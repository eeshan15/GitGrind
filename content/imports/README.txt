Import manifests.

Every run of tools/import_questions.py writes one JSON file here recording
exactly what it found, matched, held for review and rejected, plus the
coverage gaps that remained afterwards. Safe to delete; kept so you can
audit an old import months later.
